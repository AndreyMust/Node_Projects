<!DOCTYPE html>
<html>
<head>
    <title><%=title %></title>
    <meta charset="utf-8" />
</head>
<body>
     
    <h1><%=title %> в EJS</h1>
     
    <% if(emailsVisible) {%>
        <h3>Ёлектронные адреса</h3>
        <ul>
            <% for(var i=0; i<emails.length;i++) {%>
                <li><%=emails[i] %></li>
            <%} %>
        </ul>
    <% }else {%>
        <h3>Ёлектронный адрес отсутствует</h3>
    <%} %>
    <p>“елефон: <%=phone %></p>
</body>
<html>